# Used Agent: Claude Sonet 4.5

# Prompt 1 - Creation of the pipeline

As an expert DEvOps engineer, create a pipeline using github actions to perform the next actions:
1. Move to the backend and run the tests with "npm test" and report the results
2. Build the backend code running "npm run build" and report the results
3. Deploy the react project to an EC2 instance at AWS.

The pipeline code must be generated into the pipeline.yml file


# Prompt 2 - Change to Linux specification
correct the pipeline be based on linux and not ubuntu.

# Prompt 3 - Fixing error
got this error at Job 3 "# ***:22 SSH-2.0-OpenSSH_8.7 ... [error stack trace]

# Prompt 4 - Fixing deploy error to EC2
during the deploy to Backend to EC2 Im getting this error npm ERR! code ENOENT.... [error stack trace]

